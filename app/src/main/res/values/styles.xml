<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="Base.Theme.Personal_Finance_App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
    </style>

    <style name="TextButtonStyle" parent="@android:style/Widget.Button">
        <!-- Make the background transparent -->
        <item name="android:background">@android:color/transparent</item>
        <!-- Remove default padding and elevation -->
        <item name="android:padding">0dp</item>
        <item name="android:elevation">0dp</item>
        <!-- Customize text appearance -->
        <item name="android:textSize">34sp</item>
        <item name="android:fontFamily">@font/jersey</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">true</item>
    </style>
        <style name="CustomSpinnerStyle">
            <item name="android:spinnerMode">dropdown</item>
            <item name="android:dropDownWidth">match_parent</item>
            <item name="android:popupBackground">@android:drawable/btn_dropdown</item>
        </style>


    <style name="CustomSpinnerItemStyle">
        <item name="android:fontFamily">@font/pixelify_sans</item>
    </style>
</resources>